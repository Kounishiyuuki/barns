/// Lightweight summary shown on the home screen.
/// All values are placeholders for the MVP source skeleton.
struct HomeSummary: Equatable, Sendable {
    let welcomeMessage: String
    let registeredItemCount: Int
    let nextCareLabel: String
    let supportGuidance: String
    let patternsEntryLabel: String
}
