# androidTest (instrumented tests)

Instrumented / Compose UI tests that need an Android runtime.

- Compose screen rendering (including image-null states)
- navigation flows once added

Source-level skeleton: tests are added alongside their features.
