# test (unit tests)

Local JVM unit tests. Mirrors the iOS test priorities:

- domain use cases
- repository / mapper conversions
- view model UI state
- image-null rendering data
- local-first save/restore

Source-level skeleton: tests are added alongside their features.
